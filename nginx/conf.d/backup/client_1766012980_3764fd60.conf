server {
    listen 80;
    server_name superjuan.miapp.com;

    # Redirigir HTTP a HTTPS (descomentar cuando tengas SSL)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://app:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # Certbot challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}

# Configuración HTTPS (descomentar cuando tengas SSL)
# server {
#     listen 443 ssl;
#     server_name superjuan.miapp.com;
#     
#     ssl_certificate /etc/letsencrypt/live/superjuan.miapp.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/superjuan.miapp.com/privkey.pem;
#     
#     location / {
#         proxy_pass http://app:5000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
